# Backend API base URL (must include /api if your server mounts routes there)
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Stripe publishable key (pk_test_... or pk_live_...) — required for checkout
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
